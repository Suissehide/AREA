import React, { Fragment } from "react";
import { StyleSheet, View } from 'react-native';
import { withWidget } from "../core/Context";

export default withWidget(({ setEmail, email }, props) => (
    <View >
        <Fragment>

        </Fragment>
    </View >
));
