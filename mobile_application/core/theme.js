import { DefaultTheme } from 'react-native-paper';

export const theme = {
    ...DefaultTheme,
    colors: {
        primary: '#FFD430',
        secondary: '#414757',
        error: '#f13a59',
    },
};
